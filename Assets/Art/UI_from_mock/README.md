# UI slices from the style mock

Cropped from `docs/design/dc411063-…png` (the "Speech & Support" mock, 1625×968).

**These are PLACEHOLDER / REFERENCE, not production assets:**
- Text, numbers, and card art are **baked in** — not editable, not reusable as clean frames.
- No alpha, no 9-slice — panels won't stretch cleanly at other sizes.
- Low-res (they're sub-regions of a 1625px render).

Use them to dress a prototype scene fast and as exact color/shape reference. Real UI = author clean, 9-sliceable panels per `docs/art-bible.md`. A few boxes are approximate (cards clipped, minor neighbor bleed) — re-crop on request.
