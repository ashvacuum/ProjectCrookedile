{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "staged=$(git diff --cached --name-only --diff-filter=ACM | grep '\\.cs$'); [ -n \"$staged\" ] && echo \"$staged\" | xargs csharpier format && echo \"$staged\" | xargs git add; exit 0",
            "statusMessage": "Formatting staged .cs files with CSharpier...",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
